from exemplo_pacote.mat import soma as s  # de exemplo_pacote.mat importe (ou busque) soma como s


print(s(4, 5))

"""O codido de importe esta referenciando o local onde as imformaçoes estão sendo buscada para a execução do script,
podemos dizer que 'de (da pasta diretorio) exemplo_pacote (pegue o arquivo) mat e dentro deste arquivo quero o script 
soma e que agora estou apelidando a soma de s"""

# Basicamente é isso que a primeira linha diz.
