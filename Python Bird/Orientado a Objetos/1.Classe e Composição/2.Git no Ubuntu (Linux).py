"""Em algumas distros Linux, o Git já vem instalado, então a primeira coisa que se pode fazer é abrir o terminal e
digita por 'git', para conferir se ele já vem instalado, se não tiver instalado o terminal te ajuda com uma mensagem
para resolver esse problema com um comando que instala o git em seu sistema."""


# sudo apt-get install git

"""Coloque a sua senha de admin, o 'y' para aceitar que baixe arquivos e pronto, vocẽ pode digitar git novamente e vai
retorna o texto de help do Git."""
